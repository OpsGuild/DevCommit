# Changelog

## [Unreleased]

### Added
- Added support for `--version` flag to display version information
- Introduced `process_per_directory_commits_from_paths` function to handle directory mode when `--files` is used
- Added `version` key to `CommitFlag` dictionary

### Changed
- Modified `detect_staged_files` function to handle cases where no staged changes are found and provide options to generate changelog or stage all changes
- Updated `main` function to handle `--version` flag and display version information
- Changed `commit_changes` function to accept `files` parameter for committing specific files
- Modified `process_per_directory_commits` function to handle directory mode and commit files accordingly
- Updated `process_per_file_commits` function to handle file mode and commit files accordingly
- Changed `process_per_related_commits` function to handle related commits and group files accordingly

### Fixed
- Fixed issue where error messages were not being filtered out correctly in `sanitize_commit_messages` function
- Fixed issue where commit messages were not being generated correctly in `process_per_related_commits` function

### Removed
- Removed `test_sanitize.py` file as it is no longer needed

### Deprecated
- None

### Security
- None
