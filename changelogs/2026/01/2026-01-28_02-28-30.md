# Changelog

## [Unreleased]

### Added
- `--version` flag to display version information

### Changed
- Improved code formatting for better readability
- Updated `parse_arguments` function to include `--version` flag
- Modified `main` function to handle `--version` flag and display version information
- Changed `process_per_directory_commits` and `process_per_file_commits` functions to use consistent naming conventions
- Updated `process_per_related_commits` function to improve code readability and reduce duplication

### Fixed
- Fixed issue with empty commit messages not being filtered out correctly
- Resolved problem with error messages not being handled properly in certain cases

### Removed
- Removed `test_sanitize.py` file as it is no longer necessary

### Deprecated
- None

### Security
- None